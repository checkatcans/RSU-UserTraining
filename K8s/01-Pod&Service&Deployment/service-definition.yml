apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: test01-restricted
  labels:
    app: myapp
spec:
  type: NodePort
  selector:
    app: myapp
    type: front-end
  ports:
    - port: 80
      targetPort: 80
