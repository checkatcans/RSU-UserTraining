apiVersion: v1
kind: Pod
metadata:
  name: cuda-test
  namespace: test01-restricted
spec:
  restartPolicy: Never
  containers:
  - name: cuda-test
    image: nvcr.io/nvidia/k8s/cuda-sample:vectoradd-cuda11.7.1-ubuntu20.04
    resources:
      limits:
        nvidia.com/gpu: "1"
